---
title: "R basics"
execute: 
  kernel: dataviz

author: "Julien Gagneur"
subtitle: "Data Analysis and Visualization in Python"
format: html
# jupyter: python3

header-includes:
  - \setbeamerfont{section title}{size=\Large} 
  - \usepackage{float} \floatplacement{figure}{H} 
---

<!-- # (PART) Get {-} -->

# R basics

## Environment

Python is a versatile and widely used programming language in data science. It supports interactive coding, allowing you to write and test code step by step while immediately seeing the results. Interactive tools like Jupyter Notebooks and Google Colab make it easy to combine executable Python code with formatted text (Markdown), data visualizations, and explanations—all in one organized environment.


```{python}
import numpy as np
import matplotlib.pyplot as plt
```

```{python echo=FALSE}
#| label: plotnine-demo
#| fig-cap: "A plotnine example"
#| fig-width: 2
#| fig-height: 2
#| echo: false
from IPython.display import display
# set backend early if headless (optional)
import matplotlib
matplotlib.use("Agg")

from plotnine import ggplot, aes, geom_point, theme_minimal
import pandas as pd

df = pd.DataFrame({"x":[1,2,3,4], "y":[3,1,4,2]})
p = (ggplot(df, aes("x","y")) + geom_point(size=4) + theme_minimal())

# important: print the plot object so Quarto captures it
display(p)
```

```{python}
x = np.random.random(100)
y = np.random.random(100)

plt.plot(x, y)
```

```{python}
print("hello")
```

```{python, eval=FALSE}
print("hello")
```

This document is written as a Quarto (`.qmd`) notebook with executable Python code chunks. Use a Jupyter/Quarto/VS code environment to run the examples interactively.